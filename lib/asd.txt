Container(
    width: double.infinity,
    child: FutureBuilder<List<EventsModel>>(
        future: eventFuture,
        builder: (context, snapshot) {
        if (snapshot.hasData) {
            List<EventsModel>? eventFuture = snapshot.data;
            return Column(
            children: [
                Row(
                children: [
                    Image.asset("assets/images" +
                        eventFuture![1].toString()),
                    Image.asset("assets/images" +
                        eventFuture[2].toString()),
                ],
                ),
                Row(
                children: [
                    Image.asset("assets/images"+eventFuture![3].toString()),
                    Image.asset("assets/images"+eventFuture[4].toString()),
                ],
                )
            ],
            );
        } else {
            Center(
            child: JumpingDotsProgressIndicator(
                color: Colors.white,
                numberOfDots: 3,
                dotSpacing: 3,
                fontSize: 5,
            ),
            );
        }
        },
    ),
    )